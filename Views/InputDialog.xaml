<Window x:Class="KenshiModManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KenshiModManager.Views"
        xmlns:properties="clr-namespace:KenshiModManager.Properties"
        Title="{x:Static properties:Resources.InputDialog_Title}" Height="200" Width="450" WindowStartupLocation="CenterOwner" Background="#1E1E1E"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow" ShowInTaskbar="False">
  <FrameworkElement.Resources>
    <Style x:Key="ModernButtonStyle" TargetType="{x:Type Button}">
      <Setter Property="Control.Background" Value="#2D2D30"/>
      <Setter Property="Control.Foreground" Value="#CCCCCC"/>
      <Setter Property="Control.BorderThickness" Value="0"/>
      <Setter Property="Control.Padding" Value="15,10"/>
      <Setter Property="Control.FontSize" Value="13"/>
      <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
      <Setter Property="Control.Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Control.Background}" CornerRadius="4"
                    Padding="{TemplateBinding Control.Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="UIElement.IsMouseOver" Value="True">
          <Setter Property="Control.Background" Value="#3E3E42"/>
        </Trigger>
        <Trigger Property="UIElement.IsEnabled" Value="False">
          <Setter Property="UIElement.Opacity" Value="0.5"/>
          <Setter Property="FrameworkElement.Cursor" Value="Arrow"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ModernButtonStyle}">
      <Setter Property="Control.Background" Value="#0E639C"/>
      <Setter Property="Control.Foreground" Value="White"/>
      <Style.Triggers>
        <Trigger Property="UIElement.IsMouseOver" Value="True">
          <Setter Property="Control.Background" Value="#1177BB"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </FrameworkElement.Resources>
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="PromptTextBlock" Grid.Row="0" Text="Enter value:" FontSize="14" Foreground="#CCCCCC"
               Margin="0,0,0,10"/>
    <TextBox KeyDown="InputTextBox_KeyDown" x:Name="InputTextBox" Grid.Row="1" FontSize="14" Padding="10,8"
             Background="#2D2D30" Foreground="White" BorderBrush="#3E3E42" BorderThickness="1"
             VerticalAlignment="Top" Height="35" MaxLength="100"/>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
      <Button Click="CancelButton_Click" Content="{x:Static properties:Resources.InputDialog_Cancel}" Width="100" Margin="0,0,10,0"
              Style="{StaticResource ModernButtonStyle}"/>
      <Button Click="OkButton_Click" Content="{x:Static properties:Resources.InputDialog_OK}" Width="100" Style="{StaticResource PrimaryButtonStyle}"
              IsDefault="True"/>
    </StackPanel>
  </Grid>
</Window>